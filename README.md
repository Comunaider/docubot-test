# docubot-test
Sandbox for DocuBot testing


## Recent Updates (Auto-generated)
A new function, `getUser(id)`, has been added to `src/api.js`. This function allows for the retrieval of a user's data from the database by their ID, providing a new method for fetching user-specific information.